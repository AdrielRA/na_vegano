class UserModel {
  String uid, name, email, street, num, district, city, uf, password;

  UserModel(
      {this.uid,
      this.name,
      this.email,
      this.street,
      this.num,
      this.district,
      this.city,
      this.uf,
      this.password});
}
